---
name: contributing
title: 기여하는 법
type: guide
parent: universe
related: [laws, observatory, log, nebula]
description: 우주에 무언가를 더할 때 지켜야 할 것 — 법칙·별틀·플러그인·스테이지를 늘리는 법과, 기여가 통과해야 하는 관문.
---

# 기여하는 법

이 저장소의 규칙은 하나로 요약된다.

> **말로 주장하지 말고, 재는 법을 같이 내라.**

새 규칙·새 기능·새 문장을 더할 때 「이렇게 하는 게 좋다」만 적으면 그것은 **아무도 지키지
않는 문장**이 된다. 우주는 지켜지는지 기계가 확인할 수 있는 것만 법으로 받는다.

## 0. 먼저 읽을 것

| 무엇 | 어디 |
|---|---|
| 다섯 층이 무엇인지 | [README](README.md) |
| 무엇을 안 하기로 했는지 | [법칙들](laws/) — 특히 [관측](laws/observation.md) |
| 지금까지 무엇을 쟀는지 | [log/](log/) · [docs/05-expectations.md](docs/05-expectations.md) |
| 아직 안 푼 것 | [nebula/](nebula/) |
| **npm 에 올리는 법** | [docs/10-publishing.md](docs/10-publishing.md) — ⛔ 그 문서를 안 읽고 `npm publish` 를 치지 마라 |

## 1. 관문

**먼저 커밋 관문을 켜라.** 한 번만 하면 된다.

```bash
npm link                                  # `universe` 를 잇는다 (한 번만)
universe hooks --install                  # git config core.hooksPath .githooks
```

⛔⛔ **되돌릴 때는 `npm rm -g universe-harness` 다 — `npm rm -g universe` 가 아니다.**
`npm rm -g` 가 받는 것은 **패키지 이름**이고, 이 저장소는 이름이 둘이다:

| | 이름 | 어디서 쓰나 |
|---|---|---|
| **패키지** | `universe-harness` | `npm i` · `npm rm -g` · `package.json` |
| **명령** | `universe` | 터미널에서 치는 것 · 문서의 모든 `universe X` |

⚠️ npm 의 `universe` 는 **남의 패키지**다(crossfilter/universe). 명령 이름으로 지우려 하면
아무것도 안 지우거나 **남의 것을 건드린다.** 이름이 갈린 것은 배포하면서 생긴 일이라
[배포 가이드](docs/10-publishing.md) 에도 같은 표가 있다.

⛔ `npm link` 를 안 하면 `universe` 는 **없는 명령**이다 — 그때는 아래의 `universe X` 를
`node bin/universe.mjs X` 로 바꿔 친다(도구도 이어져 있지 않으면 **그 형태로** 알려 준다).

커밋마다 빠른 것만 본다 — 우주 형식 · 문서 링크 · 부품 시험 · 엔진 계약 규칙 ·
행동 계약 보호. 급할 때 `git commit --no-verify` 로 건너뛸 수 있지만 `round close` 가
다시 문다.

⛔ **여기 시간을 적지 않는다(R123).** 「약 1.5초」라고 적혀 있었는데 실측은 2.8초였고
지금은 3.6초다 — **훅이 끝날 때 자기 시간을 스스로 찍는다.** 적으면 또 낡는다.
(`lib/delivered.mjs` 가 같은 규율을 적어 뒀는데 **이 자리는 놓쳐 있었다** — 손 목록은 한 자리만 고쳐진다.)

⚠️ **왜 커밋 시점인가**: 관문이 `round close` 에만 있던 때, `a11y/img-alt` 의 패턴에
`NEVERMATCH` 가 커밋돼 **그 규칙이 아무것도 잡지 못하는 상태로 HEAD 에 있었다.**
라운드를 닫을 때 잡는 것은 늦다.

### 보내기 전에 이것부터

```bash
cd observatory/engine && npm install && npm run build && cd ../..   # 엔진이 없으면 절반을 못 잰다
universe check                                          # 관문 **전부**
```

⛔ **관문을 여기 나열하지 않는다.** 예전엔 다섯 개를 손으로 적어 뒀는데
그 사이 관문이 **열여덟 개**가 됐다(실측 R85) — 목록은 낡고, 낡은 목록은
「이것만 돌리면 된다」는 거짓말이 된다. 목록은 `lib/gates.mjs` 한 자리에 있고
`universe check` 와 `round close` 가 **같은 것**을 읽는다.

⚠️ 못 재는 것은 **왜 못 재는지** 말한다(엔진이 아직 없어서 · 배달본이라 · 파일이 없어서).
「빨간불 0개」와 「7개는 못 쟀다」는 다른 말이다.

⛔ **파이프 뒤에서 종료코드를 읽지 마라.** `cmd | tail` 의 `$?` 는 `tail` 의 것이라
**실패가 성공으로 보인다.** 이 저장소는 그 실수로 두 번 당했고, 그래서 그것이
[관측 법칙 §3](laws/observation.md) 이 되었다. 이렇게 읽어라:

```bash
node observatory/observe.mjs > /tmp/out.log 2>&1; echo "exit=$?"
```

## 2. 법칙을 하나 더할 때

법칙은 `laws/*.md` 이고 **frontmatter 가 형식이다.** `rules:` 에 적힌 규칙 이름이
계약 엔진의 규칙과 이어져야 하고, 이어지지 않으면 `verify-laws.sh` 가 막는다.

받는 조건 — ⛔ **수를 적지 않는다**(「셋」이라 적혀 있었는데 넷이었다 · 실측 R85):

1. **재는 법이 있다** — `## ⚠️ 재는 법` 절에 실행 가능한 명령을 적는다.
2. **양방향으로 증명했다** — 어기는 코드에서 잡히고, **옳은 코드에서는 안 잡힌다.**
   한쪽만 보면 「항상 통과」와 「항상 실패」를 못 가른다. 실제로 `bg-[var(--token)]`
   처럼 **옳은 코드를 벌하던** 규칙이 있었고, 대규모(1,227 파일)에서야 드러났다.
3. **이름이나 문자를 열거하지 마라 — 의미로 잡아라**(관측 법칙 §9).
   같은 고장이 네 번 났다: `svg`·`Icon*` 만 아는 규칙, `?` 를 세다 `??` 를 오인한 규칙,
   소문자 `input` 만 보는 규칙, Tailwind 접두사를 열거한 규칙.
   **넷 다 자기 코드에서는 안 보였다.**
   ⇒ **남의 코드에 걸어 「무엇을 못 잡나」를 재라.** 「무엇을 잡았나」만 보면 눈먼 자리를 못 본다.
4. **등재하고 목차에 올린다** — `universe.config.json` 의 `laws` 배열과 `laws/README.md` 표.
   ⚠️ 둘 다 `observe` 가 막는다(R56). 파일만 만들면 **아무 은하도 켤 수 없고**,
   목차에 없으면 **있는데 아무도 모르는 법칙**이 된다.
5. **`measured:` 를 frontmatter 에 쓰지 않는다** — 수치는 재는 쪽(`observatory/`)에 산다.
   법칙 파일에 숫자를 박으면 그 숫자가 낡아도 아무도 모른다.

## 3. 별틀(`bigbang/templates/star/`)을 고칠 때

별틀이 만드는 코드는 **법칙을 지켜야 한다.** 틀이 어기면 태어나는 모든 별이 어긴다.
고친 뒤 실제로 하나 태워 보고 관문을 돌려라:

```bash
universe new <은하> <태양계> <별> > /tmp/new.log 2>&1; echo "exit=$?"
```

## 4. 스테이지를 더할 때 (에이전트가 풀 결함)

스테이지는 「빌드·린트·타입체크가 전부 초록불인데 실제로는 깨져 있는」 결함을 심는다.
초록불 아래 숨지 않는 결함은 스테이지가 아니라 그냥 버그다.

- **재현되는 것만 넣어라.** 번들러 tree-shaking 처럼 결정적이지 않은 것, 문서화되지
  않은 산출물에 기대는 것은 빼라. 재현 안 되는 결함을 심으면 에이전트는
  「고쳤다」가 아니라 **「원래 그랬다」를 초록불로 배운다.**
- **저장소 고유값을 박지 마라.** 특정 프로젝트의 의존성 목록을 하드코딩한 스테이지가
  있었고, 그 스테이지는 다른 은하에서 `boot build` 부터 깨져 **4회 연속 실패의
  진짜 원인**이었다.
- **양방향으로 증명하라** — 결함 상태에서 채점 ❌, 고친 상태에서 ✅.
  `--oracle --static-only` 로 하면 LLM 을 한 번도 안 쓴다.

## 5. 플러그인(다른 스택)을 더할 때

`observatory/engine/packages/@plugins/` 아래. **먼저 주입(`PluggableHarness`)으로 시작하라** —
채울 자리가 `executeBuildAndTest`·`provideStages`·`provideStaticRules` 셋뿐이면 상속이 필요 없다.
플러그인 객체에 없는 훅을 덮어써야 할 때만 상속으로 옮긴다.

⛔ **플러그인끼리 의존하지 마라.** 공통이 필요하면 `@core` 로 올려라.

## 5-1. 여러 갈래가 동시에 돌 때

⛔ **디렉터리 단위로 스테이징하지 마라.** `git add -A -- <디렉터리>` 는 그 순간 그 안에
있는 **남의 작업 중인 내용까지** 떠간다.

실제로 당했다. 한 갈래가 「이 시험이 정말 무는가」를 확인하려고 규칙 패턴을 일부러
망가뜨려 둔 순간, 다른 쪽이 그 디렉터리를 통째로 스테이징했다. 결과는
**`a11y/img-alt` 가 HEAD 에서 아무것도 잡지 못하는 상태로 커밋된 것**이었다.
초록불이었다 — 그 규칙을 재는 시험이 그때는 없었기 때문이다.

| 하라 | 하지 마라 |
|---|---|
| `git add <내가 고친 파일>` — 이름으로 | `git add -A` · `git add <디렉터리>` |
| 커밋 전에 `git status --porcelain` 을 눈으로 본다 | 목록을 안 보고 커밋한다 |
| 갈래에게 **범위를 파일/디렉터리로 못 박아** 준다 | 「알아서 고쳐라」 |

그리고 **갈래도 감시자를 의심하라.** 이 사고를 발견한 것은 감시자가 아니라 갈래였다 —
자기 변이를 되돌리려다 HEAD 가 이미 오염된 것을 보고 보고했다. 의심은 양방향이다.

## 5-2. 검사를 넣을 때 — **변이를 어디에 겨누는가**

⛔ **초록불은 증거가 아니다.** 넣은 검사는 **일부러 깨서** 무는지 봐야 한다.
그런데 **변이를 겨누는 것 자체가 자주 틀린다.** 한 세션에서만 **일곱 번** 다시 겨눴고,
그중 **넣은 검사가 죽어 있던 적이 두 번**이었다. 유형이 반복되므로 적어 둔다.

### ⛔ 변이가 헛것이 되는 다섯 가지

| 유형 | 무슨 일이 나나 | 어떻게 안다 |
|---|---|---|
| **① 변이가 안 들어갔다** | 정규식이 안 맞아 파일이 그대로다 — 「검사가 약하다」로 잘못 읽는다 | 변이 뒤 **파일을 다시 읽어** 바뀌었는지 봐라 |
| **② 다른 검사가 먼저 문다** | 내 검사가 아니라 옆 검사가 잡았다 — 내 검사는 여전히 안 재진다 | **거부 사유**를 읽어라. 종료코드만 보면 못 가린다 |
| **③ 문구를 겨눴다** | 화면 글자를 바꿨는데 **판정이 안 바뀐다** | ⛔ **문구는 사람이 읽는 것이지 판정이 아니다.** 종료코드가 달라지는 자리를 겨눠라 |
| **④ 초록인 자리로 비교했다** | 원래 0이라 **언제나 같다** — 비교가 무의미하다 | **죽는 자리**로 비교해라(없는 이름·빈 폴더 등) |
| **⑤ 기준선이 0이 아니다** | 변이 틀이 「깨끗한 상태에서 exit 0」을 요구해 **변이가 아예 안 돈다** | 기준선이 0인 호출을 찾거나 픽스처로 만들어라 |

### ⛔ 그리고 이 둘은 절대

- **변이가 저장소를 부수면 안 된다.** 되돌릴 수 없는 변이는 넣지 마라 —
  실측: 「우주 안에 우주 깔기」 거절을 끄는 변이가 **진짜로 우주를 깔았다.**
  ⇒ 그런 것은 `verify-checks` 에 넣지 말고 **`--dry-run` 을 같이 주는** 부품 시험으로 재라.
- **막는 것만 시험하지 마라.** 그러면 **정당한 것을 막는 관문**이 남고, 다음 사람이 관문을 통째로 끈다.
  ⇒ 「걸려야 하는 것」과 **「절대 걸리면 안 되는 것」**을 둘 다 넣어라.
  실측: 분모 가르개를 그렇게 재서 `src/dist-helper.ts` 같은 **이름에만 든 소스**를 지켰다.

⚠️ **수는 하한이지 증명이 아니다.** 「막을 것 6 · 통과할 것 7」을 세는 것으로는
**옳은 자리를 막았는가**를 못 잰다. 수는 「안 셌다」를 막지 **「틀리게 셌다」를 못 막는다.**

## 6. 커밋 메시지

한 줄 요약이 아니라 **무엇을 알게 되었는지**를 적는다. 특히:

- 내가 틀렸던 것을 고쳤다면 **무엇을 잘못 믿었는지** 적는다.
- 수치를 적으면 **어떻게 쟀는지**(명령과 종료코드)를 같이 적는다.
- 「아마 될 것이다」를 결과로 적지 않는다. 못 고쳤으면 못 고쳤다고 적는다.

## 7. 라운드 — 이 우주가 스스로 도는 바퀴

큰 변경은 라운드로 감싼다.

```bash
universe round new "무엇을 할 것인가"
# … 작업 …
universe round close     # 평가표 검증 + 최고 판정이 아닌 축을 성운으로 승격
universe round audit     # 로그(정본)와 성운(파생)이 어긋나지 않는가
```

평가표에는 **판정마다 근거**가 있어야 한다. 근거 없는 칸, 빈 판정, 여러 축을 뭉갠
합성 등급은 `close` 가 거부한다. 그리고 최고 판정이 아닌 축은 **자동으로 성운에 실린다** —
로그에만 남은 제안은 실행되지 않기 때문이다. (실제로 승격 4건이 다른 작업에 덮여
사라진 적이 있고, 그래서 `round audit` 이 생겼다.)
