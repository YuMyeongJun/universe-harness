/**
 * **배달되는 것의 목록 — 한 자리.**
 *
 * ⛔ 여기 없는 것은 소비 저장소에 가지 않는다. 은하·성운·로그는 **그 저장소의 것**이라
 * 배달되지 않는다(보존 법칙). 엔진도 안 간다 — 패키지가 들고 온다.
 *
 * ⚠️ `init` 과 `check` 가 **같은 목록**을 읽어야 한다. 두 자리에 복사하면
 * 한 자리만 늘어나 조용히 갈린다(R47 이 그 종류를 잡았다).
 */
export const DELIVERED = [
  /* ⚠️ `lib` 을 배달하지 않아 **42커밋 동안 배달본이 깨져 있었다.** 배달되는
     `observatory/*.mjs` 가 `../lib/home.mjs`·`../lib/flags.mjs` 를 import 하는데
     `lib/` 이 없어 `ERR_MODULE_NOT_FOUND` 로 죽었다. 아무도 **배달본을 돌려본 적이
     없어서** 몰랐다 — 그래서 `universe delivery` 를 만들었다. */
  ['lib', '부품 — 우주의 집 찾기 · 플래그 검사'],
  ['laws', '법칙 — 무엇이 옳은가'],
  ['orbits', '궤도 — 어떻게 반복되는가'],
  ['forces', '힘 — 누가 하는가 (명부만 · 실물은 은하마다 새로 쓴다)'],
  ['bigbang', '빅뱅 — 별을 태어나게 하는 명령과 템플릿'],
  ['observatory', '관측소 — 다섯 장치'],
  /* ⚠️ 커밋 관문을 **배달한다.** 안 배달하면 소비 저장소에는 관문이 `round close` 에만
     있고, 그건 늦다 — 죽은 규칙이 HEAD 에 며칠 산다(실측: `a11y/img-alt`).
     다만 깔기만 해서는 안 켜진다(`.git/hooks` 는 커밋되지 않는다) → 아래 안내에서 짚어 준다. */
  /* ⛔ **여기 시간을 적지 마라(R123).** 「약 1.5초」로 적혀 있었는데 실측은 2.8초였다 —
     R118 이 훅은 자기 시간을 찍게 고쳤는데 **이 줄은 놓쳤다.** 훅이 매번 찍는다. */
  ['.githooks', '커밋 관문 — 빠른 것만 (훅이 자기 시간을 찍는다)'],
  ['docs', '문서'],
];

/**
 * 배달에서 빼는 것 — **OS 부스러기**.
 *
 * ⛔ 실측(R91): `bigbang/.DS_Store` 가 소비 저장소로 따라갔고, 그 뒤 Finder 가 새 것을
 * 만들자 `universe check` 가 「**배달본이 낡았다**」고 말했다. 기계는 하나도 안 바뀌었는데.
 * 헛경보는 진짜 경보를 죽인다 — 다음에 진짜로 낡았을 때 아무도 안 본다.
 *
 * ⚠️ 배달하는 쪽(`init`)과 재는 쪽(`check`)이 **같은 목록을 봐야 한다.** 한쪽만 빼면
 * 「깔았는데 낡았다」가 영구히 뜬다.
 */
export const NOT_DELIVERED = ['.DS_Store', 'Thumbs.db', 'desktop.ini'];

/** @param {string} name 파일 이름(경로 아님) */
export const isJunk = (name) => NOT_DELIVERED.includes(name);
