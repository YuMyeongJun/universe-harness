/**
 * 두 번째 코드 폴더 — 훑는 곳이 **여럿**일 때를 재려면 하나로는 모자란다.
 * ⛔ `.tsx` 로 두지 않는다: 보존 법칙이 「별은 은하로」라며 우주 안의 `.tsx` 를 잡는데,
 *    이 픽스처는 **등록된 은하가 아니라** 탐침이 잠깐 등록했다 지우는 자리라 그 예외에 못 든다.
 */
export const panelLabel = (): string => 'panel';
