{
  "//": "픽스처 은하 설정. 실제 서비스가 아니라 스테이지를 돌리기 위한 최소 저장소다.",
  "project": {
    "appWorkspace": "",
    "appDir": ".",
    "viteConfig": "vite.config.ts",
    "appTsconfig": "tsconfig.json",
    "distDir": "dist",
    "seedComponentDir": "src/components/__harness__",
    "//deepLinks": "s02 는 CloudFront 형상을 스스로 심는다 — 이 목록만 있으면 채점이 선다. 픽스처 앱에 라우터는 없지만 이 축이 재는 것은 **정적 오리진의 폴백 규칙**이지 앱 라우트가 아니다.",
    "deepLinks": ["/detail/a1"],
    "linkPaths": ["node_modules"],
    "//initialLoadBudgetKB": "깨끗한 상태의 실측값보다 커야 한다. s01 은 이 픽스처에서 재현 불가(무거운 벤더가 없다) — 아래 README 참고.",
    "initialLoadBudgetKB": 400
  },
  "commands": {
    "install": "npm ci",
    "build": "npm run build",
    "test": "npm test",
    "//lintJson": "파이프 금지 — JSON 산출만이 증거다.",
    "lintJson": "npx eslint <TARGET> --report-unused-disable-directives --format json -o <OUT>",
    "//testFile": "s04 가 심은 폴더만 돌린다. --reporter=basic 은 vitest 3 에서 제거 예정이라 기본 리포터를 쓴다.",
    "testFile": "npx vitest run <PATH>"
  },
  "lintTargets": [{ "workspace": "", "target": "./src" }],
  "contract": {
    "//presets": "house-style 을 켠다 — s04 채점이 화살표 함수를 보고, 브리핑도 I 접두사를 약속한다. 켜지 않으면 관문이 약속과 다른 것을 잰다.",
    "presets": ["toss", "a11y", "tailwind", "house-style"],
    "model": "sonnet",
    "staticOnly": false
  }
}
