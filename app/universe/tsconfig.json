{
  "//": "별칭의 원천. `web/tsconfig.json` 이 이 파일을 extends 하고, `vite.config.ts` 의 resolve.alias 와 `components.json` 의 aliases 가 같은 이름을 쓴다. 셋이 어긋나면 타입은 통과하는데 번들이 깨진다 — 그래서 이름을 한 곳에만 적는다.",
  "//shadcn": "shadcn CLI 는 저장소 뿌리에서 tsconfig 를 찾는다. 이 파일이 없으면 `npx shadcn add` 가 별칭을 못 푼다.",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@components/*": ["web/src/components/*"],
      "@api/*": ["web/src/api/*"],
      "@routes/*": ["web/src/routes/*"],
      "@lib/*": ["web/src/lib/*"],
      "@hooks/*": ["web/src/hooks/*"]
    }
  },
  "//include": "이 파일 자체는 아무것도 컴파일하지 않는다. 실제 컴파일은 tsconfig.server.json · web/tsconfig.json 이 한다.",
  "include": []
}
