/**
 * ⚠️ **이 파일은 이제 코어의 것을 다시 내보내기만 한다.**
 * 두 플러그인에 같은 구현이 복제돼 있던 것을 `@core/fe-agent-harness` 로 올렸다.
 * 복제는 **export 이름 충돌**로 먼저 드러났다 — 같은 이름이 두 곳에서 나왔다.
 */
export { parseArgv, VALUE_FLAGS } from '@core/fe-agent-harness';
export type { IParsedArgv } from '@core/fe-agent-harness';
